import React from 'react';
import { Routes } from '~components'

/**
 * App Component
 *
 * @return {ReactElement}
 */
const App: React.FC = (): React.ReactElement => {
  // use socket io hook

  return (
    <Routes />
  )
}

export default App;
